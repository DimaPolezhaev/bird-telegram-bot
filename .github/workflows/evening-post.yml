name: Evening Bird Post

on:
  schedule:
    - cron: '0 15 * * *'
  workflow_dispatch:

jobs:
  evening-post:
    runs-on: ubuntu-latest
    steps:
      - name: Send evening bird post (18:00 МСК)
        run: |
          echo "🦜 Отправляю вечерний пост (18:00 МСК)..."
          response=$(curl -s -X POST "https://bird-telegram-bot.vercel.app/api/cron?type=evening")
          echo "Response: $response"
