name: Morning Bird Post

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch:

jobs:
  morning-post:
    runs-on: ubuntu-latest
    steps:
      - name: Send morning bird post (10:00 МСК)
        run: |
          echo "🦜 Отправляю утренний пост (10:00 МСК)..."
          response=$(curl -s -X POST "https://bird-telegram-bot.vercel.app/api/cron?type=morning")
          echo "Response: $response"
